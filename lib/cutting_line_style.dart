import 'package:flutter/material.dart';
/// 扇形分割线样式
class CuttingLineStyle {
  final double width;
  final Color color;

  const CuttingLineStyle({
    this.width = 1,
    this.color = Colors.grey,
  });
}